from libtoolapi.note import read_note
# import json

# note_id = 2

# note = read_note(note_id)
# # decodifica dati (sono bytes)
# note_data = json.loads(note.content.decode('utf-8'))

# print(f"UNO SOLO VERO? -------- {note_data}")

print('wwho sisi')